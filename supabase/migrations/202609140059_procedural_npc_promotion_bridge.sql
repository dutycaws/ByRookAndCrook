-- The package cutover owns canonical-NPC promotion. Keeping this migration
-- empty prevents the old generated-definition bridge from becoming a second
-- source of resident authority on a clean prototype reset.
begin;
commit;
