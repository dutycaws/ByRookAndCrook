<script lang="ts">import type { PageProps } from './$types'; let { data }: PageProps = $props();</script><main class="public-community"><a href="/login">By Rook &amp; Crook</a><header><p class="eyebrow">Immutable tavern record</p><h1>{data.share.npcName ?? 'Conversation record'}</h1><p>This link preserves the shared record as it was published.</p></header><article class="community-card">{#if data.share.displayName}<p>Shared by {data.share.displayName}</p>{/if}<pre>{JSON.stringify(data.share.transcript, null, 2)}</pre></article></main>
