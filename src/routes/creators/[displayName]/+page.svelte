<script lang="ts">import type { PageProps } from './$types'; let { data }: PageProps = $props(); let creator: any = $derived(data.creator as any); let npcs: any[] = $derived(data.npcs as any[]);</script><main class="public-community"><a href="/login">By Rook &amp; Crook</a><header><p class="eyebrow">Community creator</p><h1>{creator.displayName}</h1><p>{creator.bio}</p></header><section class="community-grid">{#each npcs as npc}<article class="community-card"><p>{npc.rating} · immutable version</p><h2>{npc.name}</h2><p>{npc.title}</p><p>{npc.shortDescription}</p></article>{:else}<p>No public NPCs yet.</p>{/each}</section></main>
